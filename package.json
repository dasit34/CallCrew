{
  "name": "callcrew-mvp",
  "private": true,
  "scripts": {
    "build": "cd callcrew-backend && rm -rf node_modules/.cache && npm install && cd ../callcrew-dashboard && rm -rf node_modules/.cache && npm ci && npm run build",
    "start": "node server.js"
  }
}
